# 1,000,000-Digit YCD Test Data

This folder contains YCD files split into 1,000,000-digit chunks.

The files are intentionally divided into many small parts so the tests can verify behavior across file boundaries. Keep at least files `0.ycd` through `14.ycd` available for the 6-digit boundary tests.
